victron_token=example
erp_token=example
workTLD=sentracam.com
pcuser=andrew.leibowitz
idUser=example
fishuser=example
fishpass=example
switchuser=example
switchpass=example
pveuser=example@pam
pvepass=example
# SSH to PVE host for qm commands — Linux user only (no @pam). Falls back to pveuser sans realm.
pvesshuser=root
nucuser=example
nucpass=example
scryptuserssh=example
scryptuserweb=admin
scryptpass=example
install=example-install-command
SENTRA_NETWORK_TOOL_V19_DIR=C:\Users\andrew.leibowitz\Documents\Sentra_Network_toolv19
cxtmanager=example@example.com
nocmanager=example@example.com
PAUSE_AUTOMATED_TASKS=0

# Optional runtime (set by run_live.bat / run_sandbox.bat instead of editing here)
# WORK_TOOL_ENV=live
# WORK_TOOL_PORT=5000
# WORK_TOOL_DATA_DIR=
# WORK_TOOL_URL=http://127.0.0.1:5000